#version 450

layout(local_size_x = 1) in;

layout(binding = 0) readonly buffer BufferA {
    float data[];
};

layout(binding = 1) writeonly buffer OutputBuffer {
    float out_data[];
};

void main() {
    uint idx = gl_GlobalInvocationID.x;

    out_data[idx] = idx * 2.0;
}