# Breeze TTS 2 native adaptation

Approved scope: lossless GGUF conversion, pure Rust ordinary speech, instruction-guided voice design, reference speech cloning and direction. Use existing CPU operators and dependencies only. No added BLAS, Accelerate, GPU or Python inference bridge. Original checkpoints and pinned reference checkout remain read-only.

Input: `/Users/gouzi/Documents/git/rust-model-inference/models/Breeze-TTS-2`.
Reference: breeze-tts commit `e2c5ac2f54fe15daa94237a7dbf31e446660a4c9`, Transformers 4.57.3 and Qwen TTS 0.1.1, CPU eager.

Main GGUF architecture `breeze` stores original BF16/F32 tensors and exact config/tokenizer JSON metadata. Codec GGUF architecture `breeze_audio` stores original F32 tensors/config. Native dispatch requires explicit architecture. Read nested backbone config, not stale top-level RoPE fields. Main graph: noncausal T5Gemma2 text encoder → projection → causal Qwen3 backbone → first codebook / EOS 2051 → 12-layer causal depth decoder for 15 remaining codebooks → bundled Qwen 12Hz decoder, mono 24000Hz, 1920 samples/frame. Reject reserved codec IDs 2048–2050. Reference prompt uses bundled encoder and exact transcript, audio EOS uses all-zero codebook frame. CFG applies to backbone and depth logits.

Acceptance: fail malformed config/shapes, match tokenizer IDs and checkpoint order/shapes/F32 raw bits, match repeated greedy frames, run release CLI and waveform checks. Numerical divergence is a failure to align, even with plausible audio. Report any unverified boundary explicitly.
